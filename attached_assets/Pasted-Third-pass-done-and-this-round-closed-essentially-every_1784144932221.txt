Third pass done — and this round closed essentially everything I flagged. Confirmed shipped: constraint-aware corpus lookup (the `±15` boost matching dietary/budget/party-size/preferences against attribute rows — the connective-tissue gap, now wired through `executeAgentTool`), the Yelp→Google Places switch, the `.ics` endpoint with Google-link fallback, `CONCIERGE_TIMEZONE` threaded through formatting *and* pg-boss crons, the LLM intent fallback for short-form triggers like "drinks?", the `ignored`-outcome sweep, a real test suite (9 vitest files covering exactly the regex-heavy deterministic modules I worried about), weather rescue (48h window, 60% precip threshold, once-per-plan guard), and instant group creation with genuinely careful participant resolution (ambiguous names never auto-resolve; missing phones get prompted). Plus things I didn't ask for that are smart: venue photo carousels, iMessage send styles, outbound reactions, and a delivery-status webhook where `line_blocked` auto-mutes the user everywhere and logs for the operator — that last one is the right paranoid instinct for a product whose worst failure mode is being blocked.

**(1) What's still missing:**

**The corpus is still empty, and this is now the only thing standing between you and dogfooding.** Three reviews running, the code has raced ahead of the data. `GOOGLE_PLACES_API_KEY` isn't documented as set, Sendblue secrets aren't set, and the population + human-review pass hasn't run. Notice how much shipped code is now idle without it: constraint boosting needs attribute rows to boost against, carousels need stored place IDs, tier hedging needs tiers, weather rescue needs `outdoor_seating` rows. I'd declare a feature freeze and run the launch checklist: set the keys, populate 2-3 neighborhoods, work the `/venues` review queue, wire real Sendblue creds, and put the concierge into your own friend group threads this week.

**Live-traffic assumptions are untested.** Everything's been verified via direct webhook POSTs, which means the most fragile logic — `parseTapback` string formats, group_id stability across member changes, how carousels and send styles actually render, Sendblue's real webhook payload shapes — is built against documentation, not traffic. Docs lie; the first week of dogfood will surface a batch of these, which is another reason to start now.

**No turn-quality review loop.** Your original pitch had HITL at its core, and the dashboard has approvals — but there's no way to review what the *agent said* and tag failures. A dead-simple "recent turns" feed in the dashboard (reply + context + thumbs-up/down + failure tag) turns your dogfood weeks into structured prompt-improvement fuel instead of vibes. This is the cheapest highest-leverage addition before launch.

**Small ones:** the replit.md Gotchas section still references `YELP_API_KEY` (the code moved to Google Places — stale doc line that will confuse the next session); and proactive sends have frequency caps but no time-of-day gate — a plan reminder scheduled relative to a late event can fire at 11pm. A quiet-hours check (no proactive sends before ~9am / after ~9pm local) in the delivery path is a ten-line trust protector.

**(2) New magic, exploiting infrastructure this round added:**

**Send a contact card first.** On the first-ever DM, attach a vCard so the concierge gets a name and avatar in the person's Contacts. Right now it's an anonymous number — the single biggest "is this spam?" signal working against you. One media attachment converts it into a named presence in their phone forever. Highest trust-per-line-of-code available.

**Photo-first voting.** You built carousels and tapback voting separately — fuse them. Options go out as one photo bubble per venue, a tapback on the photo is the vote. Choosing a restaurant by heart-ing a picture is a fundamentally better decision UX than reading names, feels Tinder-native, and it's mostly wiring you already have.

**Celebration moments.** Send styles exist — point them at emotional peaks: confetti when a poll resolves or a booking confirms, balloons on an occasion-reminder for a birthday. Zero new infrastructure; pure delight at exactly the moments people screenshot.

**A contextual group intro.** The intro line when the concierge joins a group is static. Its first message is your one guaranteed wow-window: read the recent thread, and if planning talk is already happening, open with something concrete — "Sounds like Saturday dinner is in the works — want three options that work for everyone?" A generic bot intro gets ignored; a bot that walks in mid-conversation already useful gets talked *to*.

**Still open from last round, unchanged in ranking:** the day-of logistics bundle (reuse `reservation_reality`/`bar_while_waiting` in reminders — cheapest of these), surprise mode, traditions, and year-in-review. I'd slot the four new ideas above ahead of surprise mode and behind the dogfood checklist.

The honest headline: the codebase is no longer the bottleneck — operations are. If it'd help, I can draft the launch-week checklist as a doc (keys, population runbook, review-queue workflow, dogfood instrumentation), since that's the actual critical path now.